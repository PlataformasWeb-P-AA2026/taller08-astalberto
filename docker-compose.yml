services:

  mariadb:
    image: mariadb:11
    container_name: orm_mariadb_uso
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_USER: user
      MARIADB_PASSWORD: password
    ports:
      - "3310:3306"
    volumes:
      - ./data/mariadb:/var/lib/mysql
